ctrl_interface=/run/wpa_supplicant
ctrl_interface_group=0
update_config=1

# Configure com: wifi-setup <SSID> <senha>
# Exemplo de entrada gerada:
# network={
#     ssid="MinhaRede"
#     psk=...hash...
# }
