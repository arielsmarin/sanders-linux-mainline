# Sanders: so esperar wlan0 ficar online. Sem isso o wait-online espera
# TODOS os links (usb0/en*/eth*) que nunca sobem -> timeout -> degraded.
[Service]
ExecStart=
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --interface=wlan0
